<h1>Edit an Artist</h1>

<%= form_for(@artist) do |f| %>
  <p>Update <%= f.label :name %>: <%= f.text_field :name%></p>
    <p>Update <%= f.label :bio %>: <%= f.text_field :bio %></p>
  <%= f.submit %>
<% end  %>
